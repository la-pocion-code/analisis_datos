# Entorno virtual
virtual-env/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Variables de entorno
.env
# ⚠ Y sus copias: un `.env.bak-<fecha>` lleva los MISMOS secretos y no lo cubria
# la linea de arriba, asi que un `git add -A` lo habria publicado en GitHub.
.env.*
!.env.example

# Jupyter
.ipynb_checkpoints/

# Datos sensibles (no versionar)
*.xlsx
*.xls
*.csv
*.pdf

# PyInstaller
build/
dist/
*.spec
*.exe

google_credentials.json
.logs/
*.log
*.jiff
*.jfif
# Salida del skill graphify (cache de AST, se regenera)
graphify-out/
