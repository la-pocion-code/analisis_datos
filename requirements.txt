pandas>=2.0.0
numpy>=1.24.0
psycopg2-binary>=2.9.10
python-dotenv>=1.0.0

# ⚠ Las de abajo YA se usaban y NO estaban declaradas. No dio problema porque
# `run_dw.py` no importa los loaders de Drive, así que el worker de Railway nunca
# las necesitó — pero cualquiera que corriera `cargar_bi_datasets.py` o
# `cargar_mapeos.py` allí se habría encontrado un ImportError. Al enganchar
# `cargar_marketing` al cron (paso 2b de run_dw.py) esto pasa a ser un requisito
# de arranque de verdad.
openpyxl>=3.1                   # pd.read_excel — los tres loaders de Drive
google-auth>=2.28               # classes/drive_loader.py
google-api-python-client>=2.120 # classes/drive_loader.py (Drive v3)

# Marketing: la TRM de datos.gov.co, y los conectores de Supermetrics y Shopify
# cuando existan sus credenciales. GA4 y Search Console van por las de Google.
requests>=2.31
